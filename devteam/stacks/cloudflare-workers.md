# Stack pack — cloudflare-workers

Platform hazards for Cloudflare Workers / Pages / D1 / R2 / Queues / wrangler. Near-empty by design (LEAD-4): rules land here the day they are learned, never dumped into a seat file.

`CF-WORKERS RULES v1 LOADED`

- **CFW-1** Pages demo sites are **direct-upload**: a git push does NOT deploy — `wrangler pages deploy` and then verify the live URL.
